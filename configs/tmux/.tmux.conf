# Claude Code: preserve notifications, progress updates, and modified key sequences.
set -g allow-passthrough on
set -s extended-keys on
set -as terminal-features 'xterm*:extkeys'
set -g focus-events on
