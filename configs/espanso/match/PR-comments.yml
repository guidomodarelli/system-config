matches:
  - triggers: ["%%PR", "%PRi18"]
    label: "Translate"
    force_clipboard: true
    replace: |
      This text does not support translation. Please use the `i18n` library instead for internationalization.