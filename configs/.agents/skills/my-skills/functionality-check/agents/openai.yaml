interface:
  display_name: "Functionality Check"
  short_description: "Check if functionality exists in codebase"
  default_prompt: "Use $functionality-check to determine if a specific feature from a spec/draft is already implemented in the current codebase."
